// The icon set moved into the shared ui-kit (used by both skins). Kept as a
// re-export so the minimal chrome's existing imports keep working.
export { default, ICON_EMOJI, type IconName } from '../../ui-kit/Icon'
